info:
  name: Get Session State
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}sessions/{{session_id}}/state"
  auth: inherit

runtime:
  variables:
    - name: session_id
      value: 1600a1d5-41cb-4122-b479-d1b89c0c805b

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
