info:
  name: Get Session Questions
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}sessions/{{session_id}}/questions"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:8000/
    - name: session_id
      value: aa3b6930-a9dd-459e-bfb4-10f497705460

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
