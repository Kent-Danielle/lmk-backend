info:
  name: Get Results
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}sessions/{{session_id}}/results"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:8000/
    - name: session_id
      value: 2d0bcafa-b19f-4f0b-80cb-0ab9cf8fd560

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
